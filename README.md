# BeautyViolett - Система управления салоном красоты
Веб-приложение для управления салоном красоты с системой отмены действий (CTRL+Z), кэшированием и резервным копированием данных.

## Основные возможности

### Управление данными
- **CRUD операции** для всех сущностей (клиенты, мастера, услуги, записи)
- **Интуитивный интерфейс** с модальными окнами
- **SPA архитектура** - вся работа в одном окне

### Система отмены действий (CTRL+Z)
- **Отмена до 8 последних действий** (только CRUD операции)
- **Хранение истории в куках и сессиях**
- **Реализация через паттерны**: Команда, Снимок, Посредник

### Кэширование и производительность
- **Интеллектуальное кэширование** 3-х самых тяжелых отчетов
- **Автоматическая очистка** устаревших данных

### Резервное копирование
- **Ручное создание бекапов** по требованию
- **Хранение в файловой системе** проекта
- **Восстановление данных** через интерфейс

### Логирование
- **Логирование действий пользователей**
- **WARN логи** выводятся в консоль
- **Другие логи** записываются в файлы

## Технический стек

### Backend
- **Ruby**: 3.2.3
- **Rails**: 8.1.1
- **База данных**: PostgreSQL

### Frontend
- **HTML5**, **CSS3**, **JavaScript (ES6+)**
- **Адаптивный дизайн** (responsive)
- **SPA (Single Page Application)** архитектура
- **Модальные окна** для взаимодействия

### Паттерны проектирования
| Паттерн | Назначение |
|---------|------------|
| **Команда (Command)** | Реализация системы отмены действий |
| **Снимок (Memento)** | Сохранение состояний для отмены |
| **Посредник (Mediator)** | Координация команд отмены |
| **Синглтон (Singleton)** | Единый экземпляр сервиса кэширования |
| **Прокси (Proxy)** | Кэширование запросов к отчетам |
| **Шаблонный метод** | Шаблоны генерации отчетов |
| **Стратегия (Strategy)** | Различные алгоритмы отчетности |
| **Фабрика (Factory)** | Создание объектов команд |
| **Цепочка обязанностей (Chain Responsibility)** | Логгирование |

## Быстрый старт

### Предварительные требования
- Ruby 3.2.3 или выше
- PostgreSQL 12+
- Git

### Установка за 3 шага

1. **Клонирование репозитория:**
```bash
git clone <repository-url>
cd ruby_command_project/project
```

2. **Автоматическая настройка БД:**
```bash
chmod +x db/create_database.sh
./db/create_database.sh
```

*Скрипт автоматически выполняет:*
- Создание пользователя PostgreSQL
- Настройку прав доступа
- Создание базы данных
- Выполнение миграций
- Заполнение тестовыми данными

3. **Запуск приложения:**
```bash
bin/rails server
```

Приложение доступно по адресу: **http://localhost:3000**

## Архитектура проекта

### Модели данных
```ruby
# Клиент (Client)
# - client_id: serial
# - first_name: text
# - last_name: text 
# - patronymic: text
# - phone_number: text
# - registration_date: date

# Мастер (Master)
# - master_id: serial
# - first_name: text
# - last_name: text
# - patronymic: text
# - phone_number: text
# - is_active: boolean

# Услуга (Service)
# - service_id: serial 
# - title: text
# - duration: interval
# - base_price: integer
# - category: text

# Запись (Booking)
# - booking_id: serial
# - client_id: integer
# - master_id: integer
# - service_id: integer
# - date_service: date
# - price: integer
# - status: text
# - created_at: date
# - notes: text
```

### Ключевые компоненты

#### 1. Система отмены действий (`EventMediator`)
```ruby
# Реализует паттерны Команда, Снимок, Посредник
# Хранит историю из 8 последних действий
# Восстанавливает состояние через Memento
```

#### 2. Кэширующий прокси (`ProxyCachedReportService`)
```ruby
# Синглтон для кэширования отчетов
# TTL: 10 минут
# Кэширует 3 типа отчетов:
# 1. Эффективность мастеров
# 2. Аналитика клиентов  
# 3. Операционная эффективность
```

## Использование

### Интерфейс
- **Главная страница**: Таблица с данными (клиенты/мастера/услуги/записи)
- **Модальные окна**: Создание, редактирование, удаление, поиск по ID
- **Панель инструментов**: 
  - Кнопка "Назад" (CTRL+Z)
  - Кнопка "Создать бекап"
  - Кнопка "Восстановить из бекапа"

### Горячие клавиши
- **CTRL+Z**: Отмена последнего действия
- **ESC**: Закрытие модального окна
- **Enter**: Подтверждение действия

### Создание бекапа
1. Нажмите кнопку "Создать бекап"
2. Файл сохранится в `project/backups/`
3. Имя файла: `backup_ГГГГММДД_ЧЧММСС.json`

### Восстановление из бекапа
1. Нажмите "Восстановить из бекапа"
2. Выберите файл из списка
3. Подтвердите восстановление

## 🔧 API (RESTful)

### Базовые эндпоинты
```
GET    /clients           # Все клиенты
POST   /clients           # Создать клиента
GET    /clients/:id       # Клиент по ID
PUT    /clients/:id       # Обновить клиента
DELETE /clients/:id       # Удалить клиента

GET    /masters           # Все мастера
POST   /masters           # Создать мастера
# ... аналогично для services и bookings
```

### Отчеты (с кэшированием)
```
GET    /reports/master
GET    /reports/client
GET    /reports/revenue
```

### Системные операции
```
POST   /backup/create     # Создать бекап
POST   /backup/restore    # Восстановить из бекапа
POST   /undo              # Отменить последнее действие
```

### Уровни логирования
- **DEBUG**: Детальная отладка (файл)
- **INFO**: Информационные сообщения (файл)
- **WARN**: Предупреждения (консоль + файл)
- **ERROR**: Ошибки (консоль + файл)

